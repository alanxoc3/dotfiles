#!/bin/zsh
FALLBACK_DIMENSION=$(wlr-randr --json | jq -r "\"0,0 \" + (.[0].modes[0].width | tostring) + \"x\" + (.[0].modes[0].height | tostring)")
wl-mirror -r "$(slurp 2>/dev/null || echo $FALLBACK_DIMENSION)"
